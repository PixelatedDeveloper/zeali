{{!< default}} {{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}
  <div class="col-12">
  {{#tag}}
    <p><span class="start-mark">{{@site.title}}:~$</span> cd tags/{{slug}}</p>
    <p><span class="start-mark">{{@site.title}}:~</span><span class="folder-color">/tags/{{slug}}</span> ls -l</p>
    <p>-rwx------ {{#primary_author}}{{name}} {{name}}{{/primary_author}} <time datetime="{{date format="YYYY-MM-DD H:mm"}}">{{date format="DD MMMM YYYY H:mm"}}</time>{{title}}.md</p>
  {{/tag}}

  <div class="main-content">
    {{> 'post/card-basic'}}
  </div>
  </div>