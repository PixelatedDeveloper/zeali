<div class="row">
  {{#get "posts" limit="all"}}
  {{#foreach posts}}
  <div class="card">
    <div class="col">
      <div class="card-block">
        <h2 class="card-title"><a href="{{url}}" title="{{title}}">{{title}}</a></h2>
      </div>
    </div>
  </div>
  {{/foreach}}
  {{/get}}
</div>